__pycache__/
*.py[cod]
presets/

# Local agent instructions and working notes
docs/
AGENTS.md
CLAUDE.md
CONTRIBUTING.md

# Local secrets — never commit, never publish
.env
.env.*
!.env.example

# Build artifact for Comfy Registry
node.zip

# Per-user UI state (toggles, folder, favorites filter)
user_settings.json
wildcard_favorites.json
civitai_updates.json

# Source recordings for README GIFs (published copies live in media/)
움짤/
